import { SeenObserverDirective } from './seen-observer.directive';

describe('SeenObserverDirective', () => {
  it('should create an instance', () => {
    const directive = new SeenObserverDirective();
    expect(directive).toBeTruthy();
  });
});
